dashboard {
    listeners {
        http.bind = 18083
        # https.bind = 18084
        https {
            ssl_options {
                certfile = "${OPENQTT_ETC_DIR}/certs/cert.pem"
                keyfile = "${OPENQTT_ETC_DIR}/certs/key.pem"
            }
        }
    }
}
